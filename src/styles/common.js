/**
 * @format
 * @flow strict-local
 */
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  centerContents: {
    alignItems: 'center',
    justifyContent: 'center',
  },
});
